
//cria a funcao do botao
function alterarStatus(id) {

    //cria a variavel do botao com o id (o sinal de dolar concatena o id nao pelo nome e sim pela variavel)
let gameClicado = document.getElementById(`game-1${id} `);

//variavel para pegar a div e o que for clicado (a variavel diz, game clicado pegue a classe com a variavel dashboard__item__img e pegue o elemento da pagina)
let = gameClicado.querySelector('.dashboard__item__img');
let = botao.querySelector('.dashboard__item__img');

}